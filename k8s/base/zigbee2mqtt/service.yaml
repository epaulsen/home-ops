apiVersion: v1
kind: Service
metadata:
  name: zigbee2mqtt
spec:
  type: NodePort
  selector:
    app: zigbee2mqtt
  ports:
    - name: http
      port: 8080
      targetPort: http
      nodePort: 30080
      protocol: TCP
